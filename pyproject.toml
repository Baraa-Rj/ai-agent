[project]
name = "boot-dev-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]

[tool.pyright]
pythonVersion = "3.10"

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
